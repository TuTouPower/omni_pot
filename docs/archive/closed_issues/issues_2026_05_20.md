# 已关闭问题 — 2026-05-20

> 以下为 `docs/issues.md` 中在 2026-05-20 review 后续工作中已解决的问题，归档于此。

---

## 翻译

- [X] **输入翻译与划词翻译入口仍未完全合并**：按设计稿，这两个能力应合并为同一个翻译入口；不要在欢迎页、设置或快捷键配置里继续拆成两个用户入口。（PLAN P1 #2：配置键合并、快捷键页、欢迎页、托盘均已统一）
- [X] **输入中文长句被误判为日语**：例如输入"我爱你"重复长文本时，语言检测应识别为简体中文，不应检测为日语。（PLAN P4：前端加汉字优先逻辑）

## 欢迎页

- [X] **欢迎页点击跳过后应直接关闭窗口**：用户跳过欢迎页后不要打开/保留翻译窗口，应直接关闭当前窗口。（PLAN P1 #5）
- [X] **欢迎页/快捷键提示应按平台显示修饰键**：Windows 显示 Control，macOS 显示 Command，不要把 `CommandOrControl` 原样展示给用户。（PLAN P1 #3：`format_hotkey` 全局工具函数）

## 托盘

- [X] **系统托盘快捷键提示应按平台显示修饰键**：Windows 显示 Control，macOS 显示 Command，不要展示 `CommandOrControl`。（PLAN P1 #3）

## 窗口行为

- [X] **非固定窗口失焦后应自动关闭**：除设置窗口外，其他窗口如果用户没有点击固定，应在失去焦点后自动关闭。（PLAN P1 #6：统一行为 + 移除旧配置）

## 翻译窗口 UI

- [X] **删除空格图标不符合前端 demo**：删除空格按钮应使用设计稿里的符号，不要使用自造图标。（PLAN P4：`MdSmartButton` / `CgSpaceBetween`）
- [X] **去除换行图标需要按前端 demo 确认**：不要自造图标；后续修复前先对照 demo 确认设计稿使用的具体符号。（PLAN P4）
- [X] **删除空格等文案出现原始翻译 key**：例如显示 `Delete_spaces`；需要系统检查是否还有类似未正确翻译出来的 key。（PLAN P1 #4：i18n 原始 key 暴露修复）
- [X] **词典入口命名不符合设计稿**：demo 中叫"字典/词典"，不要叫"查询字典"。（PLAN P3：用户可见名称统一为"词典"）

## 文字识别 / 截图翻译

- [X] **文字识别点击翻译与截图翻译应进入同一个截图翻译窗口**：这两个入口对应同一个窗口，统一叫"截图翻译窗口"，不要拆成两个不同窗口概念。（PLAN P1 #1：窗口模式切换）
- [X] **截图翻译窗口布局不符合 demo**：窗口应为左侧一个大的原图卡片，右侧纵向两个小卡片，分别显示文字识别内容和文字翻译内容；具体细节修复前先看 demo。（PLAN P1 #1：截图翻译右栏双卡片）
- [X] **切换识别语言后应自动重新识别**：文字识别窗口中用户切换语言后，不需要额外点击，应自动重新识别。（PLAN P1 #1：语言切换自动重执行）
- [X] **识别窗口语言区域不应显示网络图标**：语言项前不要加网络符号，按 demo 移除该图标。（PLAN P1 #1：移除语言选择器前的 Globe 图标）

## 设置 / 网络

- [X] **取消代理功能和代理相关设置**：产品不再保留代理功能；后续修复时移除所有代理相关设置项、入口、文案和行为。（PLAN P2 #7：移除代理功能）
- [X] **快捷键设置界面未和 demo 对齐**：快捷键配置页需要按前端 demo 重新核对布局、文案、分组和交互，不要沿用当前不一致实现。（PLAN P2 #8：4 项 + 按钮文案 + 子描述）
- [X] **服务设置界面未和 demo 对齐**：服务配置页需要按前端 demo 重新核对整体布局、服务卡片、字段展示和交互。（PLAN P2 #9：Tab 标签 + 按钮 + 上移/下移）
- [X] **备份功能缺少完整验证**：需要测试导出备份 zip 后能否成功再导入，并验证导入后的配置/数据是否恢复正确。（PLAN P2 #11：外部 zip 导入 + 恢复校验 UI）
- [X] **备份页面未和 demo 对齐**：备份/恢复页面需要按前端 demo 重新核对布局、文案、按钮和状态展示。（PLAN P2 #11 + P4：卡片按钮 + 测试连接 + 备份列表）
- [X] **设置界面出现原始翻译 key**：例如显示 `config.title`；需要系统检查设置窗口及其他界面是否还有未正确翻译出来的 key，并修复 fallback / locale 覆盖。（PLAN P1 #4）

## 测试

- [X] **TTS playback state e2e**：fixture 已支持 `init_script`，`translate_source_area` 和 `translate_result_cards` 已恢复完整按压/取消等待断言。（测试通过，58/58 passed）
